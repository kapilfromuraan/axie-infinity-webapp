import BundleFilter from "./filter";

const Bundles = () => {
    return (
        <div style={{ height: '100%', flexDirection: 'row', display: 'flex' }}>
            <BundleFilter />
            bundle
        </div>
    );
}

export default Bundles;